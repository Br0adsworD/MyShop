{% extends "@MyShopAdmin/index.html.twig" %}

{% block right %}
	<h1><span>Редактирование</span></h1>
	<div class="support">
	<form method="POST" action="{{path('update_category',{id_category:category.id})}}">
		{{form_widget(form,{ 'attr' : { 'class': 'someForm' } })}}
		<div class="button">
			<input type="submit" name="addProduct" value="Добавить">
		</div>
		<a href="{{ path('add_category',{'idParent':category.id}) }}">Добавить подкатегории</a>
	</form>
	</div>
	
{% endblock %}